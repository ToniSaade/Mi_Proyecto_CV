//Cómo declarar una variable:

var x;
let y;

//Cómo usar una variable:

x = 5;
y = 6;
let z = x + y;

/** Las variables en JavaScript son "case sensitives", lo cual quiere decir que "Y" e "y" son 
 * distintas variables, por ejemplo.
 */

/** Las variables definidas con "const" no pueden ser REDECLARADAS ni REASIGNADAS. Necesitan ser
 * asignadas con un valor cuando son declaradas. Por ejemplo:
 */

const IVA = 1.21